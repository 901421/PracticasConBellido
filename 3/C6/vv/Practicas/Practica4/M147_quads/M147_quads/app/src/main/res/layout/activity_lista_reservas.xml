<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- SECCIÓN DE CONTROLES RF13 ESTABILIZADA -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:orientation="horizontal"
        android:padding="8dp"
        android:gravity="center_vertical">

        <Button
            android:id="@+id/btn_open_sort"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:text="ORDENAR"
            android:layout_marginEnd="8dp"
            android:lines="2"
            android:textSize="12sp"
            app:icon="@drawable/ic_arrow_drop_down"
            app:iconGravity="textEnd"
            app:iconPadding="4dp"
            app:backgroundTint="@color/colorBtnEdit"
            style="@style/Widget.MaterialComponents.Button" />

        <Button
            android:id="@+id/btn_open_filter"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:text="FILTRAR"
            android:lines="2"
            android:textSize="12sp"
            app:icon="@drawable/ic_arrow_drop_down"
            app:iconGravity="textEnd"
            app:iconPadding="4dp"
            app:backgroundTint="@color/colorBtnEdit"
            style="@style/Widget.MaterialComponents.Button" />
    </LinearLayout>
    
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerview_reservas"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        tools:listitem="@layout/item_reserva"/>

    <!-- NUEVO: TEXTO LISTA VACÍA -->
    <TextView
        android:id="@+id/empty_view_reservas"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:gravity="center"
        android:text="@string/no_data_reservas"
        android:textColor="@android:color/darker_gray"
        android:textSize="18sp"
        android:visibility="gone"/>

</LinearLayout>